from settings.setup import *

ERROR_X = 15
ERROR_Y = 15
ERROR_Y_SPACING = 20

ERROR_COLOR = RED

OUT_OF_BOUNDS_ERROR = f"Tiles found outside of grid boundaries. Change grid size or remove tiles."

MAX_NR_PRESETS = 1
MAX_PRESETS_ERROR = f"Not all presets loaded. Editor can only hold {MAX_NR_PRESETS} presets."

MAX_NR_TILES = 11
MAX_TILES_ERROR = f"Not all tiles are loaded. Editor can only hold {MAX_NR_TILES} tiles."
